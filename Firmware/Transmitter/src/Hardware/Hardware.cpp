#ifndef __Hardware_CPP__

    #define __Hardware_CPP__ 

    #include <Hardware/Hardware.h>

    void Hardware_Tests::prueba_1(){
        
        /* -------------------------------------------------------------------------- */
        /*                      Monitor de errores inicializacion                     */
        /* -------------------------------------------------------------------------- */

            MonitorLED.setGPIO();
            
            MonitorLED.selfTest();

        /* -------------------------------------------------------------------------- */

        /* -------------------------------------------------------------------------- */
        /*                        Memoria Flash inicializacion                        */
        /* -------------------------------------------------------------------------- */

            //TODO --> Si bien funciona la memoria Flash, todavia queda pensar de forma estrategica su uso
            
            //MonitorLED.ErrorValue(Flash.selfTest());

        /* -------------------------------------------------------------------------- */

        /* -------------------------------------------------------------------------- */
        /*                             USB Inicializacion                             */
        /* -------------------------------------------------------------------------- */

            USB_TX.begin(115200);

            while (!USB_TX){   
                MonitorLED.BarridoUSB_Conectando();
            }

            if(USB_TX){

                MonitorLED.BeepBeepAll(50);
                USB_TX.println(F("USB Conectado OK"));
            }

        /* -------------------------------------------------------------------------- */

        // /* -------------------------------------------------------------------------- */
        // /*                          Tarjeta SD inicializacion                         */
        // /* -------------------------------------------------------------------------- */

            TarjetaSD.setGPIO(); 
            
            MonitorLED.ErrorValue(TarjetaSD.selfTest());
            
        // /* -------------------------------------------------------------------------- */
    
        /* -------------------------------------------------------------------------- */
        /*                          Comm: LoRa Inicializacion                         */
        /* -------------------------------------------------------------------------- */

            LoRaComm.setGPIO();
            
            MonitorLED.ErrorValue(LoRaComm.selfTest());
            
        /* -------------------------------------------------------------------------- */


    }

#endif