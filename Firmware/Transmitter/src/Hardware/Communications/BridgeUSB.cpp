 #ifndef __BridgeUSB_CPP__

    #define __BridgeUSB_CPP__

    #include <Hardware/Communications/BridgeUSB.h>

    USB_PORT_TX::USB_PORT_TX (){

    }

    void USB_PORT_TX::inicializar(){

      /*  USB_TX.begin();

        while (!USB_TX){   

            USB_PORT_TX::Error_Monitor::barridoUSB_Conectando();
        }

        if(USB_TX){

            USB_PORT_TX::Error_Monitor::beepBeepAll(50);
            USB_TX.println("USB Conectado OK");
        }*/
    }


#endif